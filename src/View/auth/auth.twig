{% extends "layout/layout.twig" %}

{% block auth %}

    {% if method is same as ("login") %}

        {% include "auth/login.twig" %}

    {% else %}
            
            {% include "auth/signup.twig" %}

    {% endif %}

{% endblock %}

